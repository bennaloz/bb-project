<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="bb_project.Client.Modules.WorkoutEditorModule.UserControls.WorkoutPlanView"
             xmlns:uc="clr-namespace:bb_project.Client.Modules.WorkoutEditorModule.UserControls"
             BackgroundColor="Red">

    <xct:Expander IsExpanded="{Binding IsActive}">
        <xct:Expander.Header>
            <Label Text="{Binding Name}" FontSize="Medium" FontAttributes="Bold"/>
        </xct:Expander.Header>
        <CollectionView ItemsSource="{Binding Workouts}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding Name}"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </xct:Expander>
</ContentView>