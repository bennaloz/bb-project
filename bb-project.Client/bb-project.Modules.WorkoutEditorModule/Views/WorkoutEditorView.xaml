<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:views="clr-namespace:bb_project.Modules.WorkoutEditorModule.Views"
             x:Class="bb_project.Client.Modules.WorkoutEditorModule.Views.WorkoutEditorView">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0" BackgroundColor="#2196F3" Orientation="Horizontal">
            <Label Text="MODIFICA SCHEDE" TextColor="White" FontSize="Medium" 
                   FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Start"/>
            <Button Text="MODIFICA" HorizontalOptions="EndAndExpand" VerticalOptions="Center" />
        </StackLayout>

        <CollectionView Grid.Row="1" ItemsSource="{Binding WorkoutPlans}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <xct:Expander IsExpanded="{Binding IsActive}">
                        <xct:Expander.Header>
                            <Label Text="{Binding Name}"
                               FontAttributes="Bold"
                                FontSize="Medium" >
                            </Label>
                        </xct:Expander.Header>
                        <views:WorkoutPlanView/>
                    </xct:Expander>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
    

</ContentView>