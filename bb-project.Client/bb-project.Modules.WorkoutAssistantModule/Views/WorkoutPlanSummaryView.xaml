<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:conv="clr-namespace:bb_project.Client.Modules.WorkoutAssistantModule.Converters"
             xmlns:wm="clr-namespace:bb_project.Client.Modules.WorkoutAssistantModule.ViewModels"
             x:Class="bb_project.Client.Modules.WorkoutAssistantModule.Views.WorkoutPlanSummaryView"
             x:Name="summaryView">

    <ContentView.Resources>
        <conv:VisibleConditionMultiConverter x:Key="visibleConverter"/>

        <Style x:Key="GridSelection" TargetType="StackLayout">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor"
                                        Value="Transparent" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor"
                                        Value="#007fff" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentView.Resources>
    
    <ContentView.Content>
      
      <!--//TODO: display workouts-->
        <StackLayout Orientation="Vertical">

            <CollectionView  SelectionMode="Single" HeightRequest="400" ItemsSource="{Binding WorkoutPlans}" SelectedItem="{Binding SelectedWorkoutPlan, Mode=TwoWay}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Horizontal" HeightRequest="50" Style="{StaticResource GridSelection}">
                            <Label Text="{Binding Name}" VerticalOptions="Center" FontSize="Medium" FontAttributes="Bold" />
                            
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Button Text="Mostra esercizi"
                    Command="{Binding ShowWorkoutExercisesCommand}"
                    CommandParameter="{Binding SelectedWorkoutPlan}"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>