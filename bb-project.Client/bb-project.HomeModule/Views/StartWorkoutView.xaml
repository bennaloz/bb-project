<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="bb_project.Client.Modules.HomeModule.Views.StartWorkoutView"
               xmlns:prism="http://prismlibrary.com">
  <ContentView.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Label Text="Next workout:" />
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*" />
                    <ColumnDefinition Width="5*" />
                </Grid.ColumnDefinitions>
                <Label Text="{Binding WorkoutPlanName, FallbackValue=WorkoutPlan}" />
                <Label Grid.Column="1"
                       Text="{Binding WorkoutName, FallbackValue=WorkoutName}" />
            </Grid>
            <Button Grid.Row="3" Command="{Binding StartWorkoutCommand}" Text="Start your workout"/>
        </Grid>
  </ContentView.Content>
</ContentView>